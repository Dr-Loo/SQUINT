# Contributing to SQUINT

Thanks for helping! This project targets Python 3.10+.

## Dev setup
```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -U pip
pip install -e .            # editable install
pip install -r requirements.txt || true
